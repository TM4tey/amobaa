<?xml version="1.0"?>
<ruleset name="Custom Ruleset"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>
        Amoba projekt PMD szabálykészlet
    </description>

    <!-- Best Practices -->
    <rule ref="category/java/bestpractices.xml">
        <exclude name="SystemPrintln" />
        <exclude name="GuardLogStatement" />
        <!-- Tesztekben gyakran használunk nem használt privát mezőket/metódusokat setup alatt -->
        <exclude name="UnusedPrivateField"/>
        <exclude name="UnusedPrivateMethod"/>
    </rule>

    <!-- Code Style -->
    <rule ref="category/java/codestyle.xml">
        <exclude name="AtLeastOneConstructor" />
        <exclude name="LocalVariableCouldBeFinal" />
        <exclude name="MethodArgumentCouldBeFinal" />
        <exclude name="OnlyOneReturn" />
        <exclude name="LongVariable" />
        <exclude name="ShortVariable" />
        <!-- DefaultPackage helyett NoPackage az új név -->
        <exclude name="NoPackage" />
        <exclude name="CommentDefaultAccessModifier" />
        <exclude name="CallSuperInConstructor" />
    </rule>

    <!-- Design -->
    <rule ref="category/java/design.xml">
        <exclude name="LawOfDemeter" />
        <exclude name="LoosePackageCoupling" />
        <exclude name="UseUtilityClass" />
        <exclude name="AvoidDeeplyNestedIfStmts" />
    </rule>

    <!-- Error Prone -->
    <rule ref="category/java/errorprone.xml">
        <!-- BeanMembersShouldSerialize törölve, mert már nem létezik ebben a formában -->
        <exclude name="BeanMembersShouldSerialize" />
        <!-- Gyakran fals pozitív -->
        <exclude name="DataflowAnomalyAnalysis" />
    </rule>

    <!-- Documentation -->
    <rule ref="category/java/documentation.xml">
        <!-- Nem kötelező mindenhez kommentet írni -->
        <exclude name="CommentRequired" />
        <exclude name="CommentSize" />
    </rule>

    <!-- Multithreading -->
    <rule ref="category/java/multithreading.xml" />

    <!-- Performance -->
    <rule ref="category/java/performance.xml" />

</ruleset>